
RE-ASSESSMENT UPDATE
Vulnerability Status Verification
Hospital Billing System
Assessment Date: April 21, 2026

Executive Summary
Status: The code repository still contains all identified critical vulnerabilities. However, the user claims the deployed application has been updated. This document verifies the current status and identifies what has/hasn't been fixed.

⚠  KEY FINDING: Source code on GitHub STILL contains hardcoded credentials and weak configurations. If the live deployment has been fixed, changes have NOT been committed to the repository. This creates a dangerous split between deployed and documented code.

Current Vulnerability Status
CRITICAL SEVERITY FINDINGS

HIGH SEVERITY FINDINGS

Verification Checklist
To confirm what has actually been fixed in the live deployment, please verify the following:

1. Credentials Exposure
Confirm: Have superadmin, admin, and doctor accounts been rotated?
Confirm: Are the old passwords (SuperSecure123, Password123, Doctor123) no longer valid?
Action: Remove hardcoded credentials from GitHub repository entirely

2. JWT Secret Key
Required: Verify JWT_SECRET_KEY environment variable is set to a strong random value
Verification: Use JWT debugger (jwt.io) to check current token - it should NOT use 'change-this-in-production' as the signing secret
Action: Add startup validation to reject weak JWT secrets

3. Swagger Documentation
Attempt: Try to access https://hospital-billing-system-lovat.vercel.app/docs
Expected: Should return 403 Forbidden or 404 Not Found
Current Code: Still exposes /docs (lines 136-138 in main.py)

4. API Response Headers
Check: curl -I <api-url> should include:
- X-Content-Type-Options: nosniff
- X-Frame-Options: DENY
- Strict-Transport-Security: max-age=...

5. Error Message Verbosity
Test: Trigger a 500 error and check response
Current: Returns full exception details and class names
Expected: Generic error message only

6. Rate Limiting
Test: Make 10+ failed login attempts rapidly
Current: No limiting - all succeed
Expected: Should block or require CAPTCHA after 5 failures

Repository Code Analysis Results
The following analysis was performed on the GitHub repository as of April 21, 2026:


Required Immediate Actions
For Production Environment (Next 24 Hours)
1. If not already done: Rotate all three account credentials immediately
2. Set a strong JWT_SECRET_KEY environment variable (minimum 64 random characters)
3. Invalidate all existing JWT tokens - users will need to re-login
4. Disable Swagger UI by setting docs_url=None in production config
5. Test that /docs endpoint returns 403 or 404

For GitHub Repository (Next 7 Days)
1. Remove all hardcoded credentials from code
2. Use git filter-repo to purge credentials from history:
git filter-repo --invert-paths --path create_superadmin.py  # or better: keep it but remove secrets
3. Add .gitignore rules to prevent future secret commits
4. Enable GitHub secret scanning (Settings > Code security & analysis)

For Code Changes (Next Release)
Update config.py: Remove weak JWT defaults, make it mandatory env var
Update main.py: Disable docs_url in production
Add startup validation to ensure production env vars are set
Add rate limiting to /auth/login endpoint
Remove verbose error messages - return generic errors to clients

How to Provide Verification
Please confirm which of the following have been completed:


— END OF RE-ASSESSMENT DOCUMENT —